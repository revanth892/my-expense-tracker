import { createSlice } from "@reduxjs/toolkit";
const expense=createSlice(
    {
        name:"expense",
        initialState:[],
        reducers:
        {
            addTransaction(state,action){}
        }
    }
);